const API = {
  baseUrl: 'https://api.themoviedb.org/3',
  apiKey: '--YOUR-KEY',
  language: 'en-US',
};

const PARAMS = {
  default: `api_key=${API.apiKey}&language=${API.language}`,
};

// export { API, PARAMS };
